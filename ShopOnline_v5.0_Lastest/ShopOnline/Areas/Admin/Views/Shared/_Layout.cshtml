<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewBag.Title - Bookstore</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
</head>
<body>
    <div class="container-fluid">
        <nav class="navbar navbar-expand-lg navbar-light" style="background-color:#b6ff00;">
            <a class="navbar-brand" href="@Url.Action("Index", "Home")">
                <img src="~/Images/open-book.png" width="30" height="30" class="d-inline-block align-top" alt="" />
                Bookstore Admin Area
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        <a class="nav-link" href="#"><i class="fal fa-chart-pie"></i> Mua nhiều nhất</a>
                    </li>
                    @if (Session["MaNhanVien"] == null)
                    {
                        <li class="nav-item">
                            <a class="nav-link" href="@Url.Action("Login", "Home")"><i class="fal fa-sign-in-alt"></i> Đăng nhập quản lý</a>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <i class="fal fa-user-circle"></i> @Session["HoTenNhanVien"].ToString()
                                @if (Convert.ToInt32(Session["Quyen"]) == 1)
                                {
                                    <span>(Quản lý)</span>
                                }
                                else
                                {
                                    <span>(Nhân viên)</span>
                                }
                            </a>
                            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                                @if (Convert.ToInt32(Session["Quyen"]) == 1)
                                {
                                    <a class="dropdown-item" href="@Url.Action("Index", "LoaiSach")"><i class="fal fa-fw fa-bookmark"></i> Loại sách</a>
                                    <a class="dropdown-item" href="@Url.Action("Index", "NhaXuatBan")"><i class="fal fa-fw fa-building"></i> Nhà xuất bản</a>
                                    <a class="dropdown-item" href="@Url.Action("Index", "NhanVien")"><i class="fal fa-fw fa-user-tie"></i> Nhân viên</a>
                                }
                                <a class="dropdown-item" href="@Url.Action("Index", "KhachHang")"><i class="fal fa-fw fa-users"></i> Khách hàng</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="@Url.Action("Index", "Sach")"><i class="fal fa-fw fa-books"></i> Sách</a>
                                <a class="dropdown-item" href="@Url.Action("Index", "DatHang")"><i class="fal fa-fw fa-file-check"></i> Đặt hàng</a>
                                <a class="dropdown-item" href="@Url.Action("Index", "DatHang_ChiTiet")"><i class="fal fa-fw fa-list-alt"></i> Đặt hàng chi tiết</a>
                                <div class="dropdown-divider"></div>
                                <a class="dropdown-item" href="@Url.Action("Logout", "NhanVien")"><i class="fal fa-fw fa-sign-out-alt"></i> Đăng xuất</a>
                            </div>
                        </li>
                    }
                </ul>
                <form class="form-inline my-2 my-lg-0">
                    <input class="form-control mr-sm-2" type="search" placeholder="Bạn muốn tìm gì?" />
                    <button class="btn btn-outline-primary my-2 my-sm-0" type="submit"><i class="fal fa-search"></i> Tìm kiếm</button>
                </form>
            </div>
        </nav>
    </div>

    <div class="container-fluid body-content">
        @RenderBody()
        <hr />
        <footer>
            <p>&copy; Bản quyền @DateTime.Now.Year bởi DH19PM.</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>